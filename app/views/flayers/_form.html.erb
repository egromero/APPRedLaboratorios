<%= form_with(model: flayer, local: true) do |form| %>
  <% if flayer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flayer.errors.count, "error") %> prohibited this flayer from being saved:</h2>

      <ul>
      <% flayer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tittle %>
    <%= form.text_field :tittle %>
  </div>

  <div class= "field">
  <%= form.label :image%>
  <%=form.file_field :image%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
